<tr>
  <td><%= link_to photo.title, users_photo_path(photo)  %></td>
  <td></td>
  <td><%= link_to 'Edit', edit_users_photo_path(photo), class: 'btn btn-sm btn-warning' %></td>
  <td><%= link_to 'Remove', users_photo_path(photo), method: :delete, data: {confirm: 'Are you sure'}, class: 'btn btn-sm btn-danger' %></td>
</tr>

