<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class='container-fluid'>
    <ul class="nav navbar-nav">
      <li class="nav-item">
        <a class='logo' href='/'>
          Marcetux
        </a>
      </li>
      <li class="nav-item">
        <div class="container-search">
          <%= search_field_tag 'query', nil, class: 'form-control', placeholder: 'Search marcetux'  %>
          <button class="icon"><i class="fa fa-search"></i></button>
        </div>
      </li>
            <li class="nav-item">
              <%= link_to 'Home', home_path, class: "nav-link #{yield(:home_active)}" %>
            </li>
      <li class="nav-item">
        <%= link_to 'Projects', photos_path, class: "nav-link #{yield(:picture_active)}" %>
      </li>
      <li class="nav-item">
        <%= link_to 'Timeline', timeline_path, class: "nav-link #{yield(:timeline_active)}" %>
      </li>
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to 'Control panel', users_photos_path, class: "nav-link #{yield(:user)}" %>
        </li>
      <% end %>
    </ul>
    <ul class="nav navbar-nav navbar-right">
        
          <% if user_signed_in? %>
            <li><%= link_to destroy_user_session_path, method: :delete do %>
              <span class="glyphicon glyphicon-log-out"></span> Logout
            <%end%>
            </li>
          <% else %>
            <li><%= link_to new_user_session_path do%>
            <span class="glyphicon glyphicon-log-in"></span> Login
            <%end%>
            </li>
        <%end%>
        
      </ul>
  </div>
</nav>